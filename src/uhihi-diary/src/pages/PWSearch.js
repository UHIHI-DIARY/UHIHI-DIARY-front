import React from 'react';
import {AuthWrapper} from "../components/auth";
import { PWSearch } from '../containers/auth';

const Auth = () => (
    <AuthWrapper>
        <PWSearch/>
    </AuthWrapper>
)

export default Auth;